# 阶段3 · 安全与质量护栏

> 来源依据：知识库 182（安全边界）、186（Reply Quality Guard）；与阶段1 落地代码严丝合缝。
> 一句话目标：**回复前**用结构化安全判断前置短路（命中 `refuse`/`crisis_support` 直接返回预设回复、不进 DeepSeek），**回复后**用纯代码规则质检（只记录不拦截，写 `metadata_json`）。
> 技术栈沿用：Hono + Cloudflare Workers + D1 + Drizzle + Zod + DeepSeek(deepseek-v4-flash，OpenAI 兼容，推理模型)。

---

## 1. 这一阶段做什么、不做什么

| 做（v1） | 不做（留给 v2） |
|---|---|
| 安全边界判断（LLM 结构化）放在**写用户消息之前**，结果写进用户消息 `metadata_json`（文章182） | 意图判断 / 情绪路由 / 回复策略模板（阶段2 范畴，安全是其前置） |
| `refuse` / `crisis_support` **前置短路**：不进 DeepSeek，直接返回预设安全回复并落 assistant 消息 | 用户可见的风险提示 UI、admin 风控面板、人工审核流 |
| `soft_boundary` / `redirect` / `caution`：把安全策略注入 system prompt 后继续正常聊天（文章182） | 多模型投票、向量检索式安全历史 |
| 安全结果联动长期记忆：`allowMemoryExtraction=false` 时跳过抽取（文章182） | `memoryPolicy: full/boundary_only/none` 细粒度策略 |
| 结构化输出退化降级：多次 prompt+zod 解析失败 → 保守 fallback（caution/soft_boundary/禁抽取）（文章182） | LangChain（项目用裸 DeepSeek，自己实现 structured output + 退路） |
| Reply Quality Guard：**纯代码规则**质检（句数/问号/建议/内部标签泄露/破坏沉浸感），只记录、写 assistant `metadata_json`（文章186） | 自动重写 / 二次生成 / LLM-as-judge / 拦截 |
| 不新增表，全部写入既有 `agent_conversation_messages.metadata_json`（文章186） | 单独的安全日志表 / 质量日志表 |

**核心原则**：安全边界 > 质量守卫；安全等级越高系统越保守；质检只观察不影响主流程。

---

## 2. 架构与 pipeline（插入点）

阶段1 主链路（`agent-chat.ts` 的 `①…⑧`）与本阶段插入点（`S1/S2/Q1`）：

```
用户发送消息
  → ②③ 读「此前」历史 + active 长期记忆          （阶段1，保持不变）
  → 【S1 安全边界判断 analyzeConversationSafety】  ★新增，必须在 ① 写用户消息之前
       输入：agentName / guardrails / activeMemories / history / latestUserText
       输出：ConversationSafety（结构化）
  → ① 写用户消息（content + status）
       ★改动：metadataJson 写入 { analysisVersion, safety }  （文章182：结果落用户消息）
  → 读 default_prompt（阶段1）
       ★改动：同步读 guardrails_prompt 供 S1 用
  → 【分流 buildBoundaryResponse(safety)】          ★新增前置短路
       ├─ refuse / crisis_support →
       │     直接生成预设文本（不进 DeepSeek）
       │     落 assistant 消息（status=completed）+ 【Q1 对预设文本也跑质检】
       │     SSE：把预设文本作为单个 delta 吐出 → done；【跳过 ⑦⑧或按 safety 决策】
       │     ⏹ return（短路结束）
       └─ continue / soft_boundary / redirect → 继续 ↓
  → ④ buildAgentChatMessages
       ★改动：注入 getSafetySystemInstruction(safety) 到 system 段（文章182）
  → ⑤ DeepSeek 流式生成（阶段1，保持不变）
  → ⑥ assistant 落库
       ★改动：先跑【Q1 evaluateReplyQuality(assistantText)】，
              metadataJson 合并 { safety, guard }（文章186）
  → ⑦ 滚动摘要（阶段1）
  → ⑧ 长期记忆抽取
       ★改动：用 safety.allowMemoryExtraction 做闸门，false 则跳过（文章182）
  → done
```

要点：安全判断**只判断不代替回复**（文章182），真正的回复仍走阶段1 链路，只受 `boundaryAction` 与 `responseGuidance` 影响。质量守卫**只记录不拦截**（文章186）。

---

## 3. 节点设计（逐节点）

### S1 · 安全边界判断 `analyzeConversationSafety`
- **输入**：`agentName`、`agentGuardrails`(string|null)、`activeMemories`、`recentMessages`(history)、`latestUserText`、`signal`、DeepSeek 配置。
- **输出**：规范化后的 `ConversationSafety`（见 §5）。
- **LLM 还是规则**：**LLM 结构化输出**。不手写关键词正则——同一句话在不同上下文含义不同，必须把 guardrails/记忆/历史一起喂给模型做判断（文章182）。
- **失败兜底**：见 §8。多次尝试都拿不到合法 JSON → 返回保守 `fallbackSafety`（`caution` / `soft_boundary` / `allowMemoryExtraction=false`），**绝不当作 safe**（文章182）。中途 `latestUserText` 为空（纯 assistant 续写场景）→ 直接返回 `safe/continue/allow=true`，不调用 LLM。

### 分流 · `buildBoundaryResponse(safety)`
- **输入**：`ConversationSafety`。**输出**：`string | null`（null 表示不短路、继续聊天）。
- **规则**：纯代码。`crisis_support` → 固定危机支持文本；`refuse` → 拒绝文本 + `responseGuidance` 安全替代方向；其余 → `null`。
- **兜底**：无（确定性函数）。

### Q1 · 回复质量守卫 `evaluateReplyQuality`
- **输入**：`assistantText`、（可选）`replyPolicy`（阶段2 才有，阶段3 传 `null` 用 `fallbackReplyPolicy`）。
- **输出**：`ReplyQualityGuard`（见 §5）。
- **LLM 还是规则**：**纯代码规则**，零额外模型调用、不影响流式速度（文章186）。检测：句子数 > 预算、问号数 > 上限、建议型表达数 > 上限、内部标签泄露、破坏沉浸感、forbidden moves。
- **失败兜底**：空文本 → `fallbackReplyQualityGuard`（status=pass、score=1、空 violations），永不抛错影响落库。

---

## 4. 数据模型（不新增表/字段，全部写 `metadata_json`）

沿用 §阶段1 `agent_conversation_messages.metadata_json TEXT`（文章186 明确：属于「消息生成时的分析结果」，不是主业务字段，先放 metadata，稳定后再拆日志表）。

### 4.1 用户消息 `metadata_json`（S1 写入）
```json
{
  "analysisVersion": "conversation-safety-v1",
  "safety": {
    "safetyLevel": "redirect",
    "category": "manipulation",
    "boundaryAction": "redirect",
    "reason": "用户请求生成操控他人情绪的策略",
    "responseGuidance": "不要提供操控话术，转为帮助用户表达真实感受、尊重对方边界。",
    "allowMemoryExtraction": false
  }
}
```

### 4.2 assistant 消息 `metadata_json`（⑥ 落库时写入，兼容阶段1 既有字段 + 安全回放 + 质检）
```json
{
  "model": "deepseek-v4-flash",
  "promptMessageCount": 7,
  "injectedMemories": 3,
  "boundaryShortCircuit": false,
  "safety": { "...": "本轮 S1 结果，便于在 assistant 侧直接回看" },
  "guard": {
    "analysisVersion": "reply-quality-guard-v1",
    "status": "warn",
    "score": 0.82,
    "sentenceCount": 4,
    "questionCount": 2,
    "adviceCount": 1,
    "violations": [
      { "code": "too_many_questions", "severity": "medium", "evidence": "回复含 2 个问句，超过上限 1。" }
    ]
  }
}
```
> 阶段1 的 assistant metadata 已有 `model / promptMessageCount / injectedMemories`，本阶段做**合并**而非覆盖。预设回复（短路分支）落库时 `boundaryShortCircuit:true`，`guard` 仍对预设文本跑一遍（预设文本天然 pass，留作格式一致性）。

Drizzle schema：**无改动**（字段已存在）。无需新建 D1 迁移（文章186）。

---

## 5. Contracts（Zod schema）

新增 `packages/contracts/src/chat/safety.contract.ts`，并在 `index.ts` 追加 `export * from "./chat/safety.contract"`。

```ts
import { z } from "zod"

/** ① 安全边界判断结果（文章182） */
export const ConversationSafetySchema = z.object({
  safetyLevel: z.enum(["safe", "caution", "redirect", "block", "crisis"]),
  category: z.enum([
    "normal", "emotional_dependency", "manipulation", "self_harm",
    "sexual_boundary", "privacy", "illegal", "medical_legal_financial", "other",
  ]),
  boundaryAction: z.enum(["continue", "soft_boundary", "redirect", "refuse", "crisis_support"]),
  reason: z.string().trim().max(300),
  responseGuidance: z.string().trim().max(600),
  allowMemoryExtraction: z.boolean(),
})
export type ConversationSafety = z.infer<typeof ConversationSafetySchema>

/** ② 回复质量守卫结果（文章186） */
export const ReplyQualityViolationCode = z.enum([
  "too_many_sentences", "too_many_questions", "too_many_suggestions",
  "internal_label_leak", "breaks_immersion",
  "forbidden_lecture", "forbidden_over_explain", "forbidden_premature_advice",
  "forbidden_intense_flirt", "forbidden_diagnosis", "forbidden_aggressive_siding",
  "forbidden_pressure", "forbidden_real_world_promise",
])

export const ReplyQualityGuardSchema = z.object({
  status: z.enum(["pass", "warn", "fail"]),
  score: z.number().min(0).max(1),
  sentenceCount: z.number().int().min(0),
  questionCount: z.number().int().min(0),
  adviceCount: z.number().int().min(0),
  violations: z.array(z.object({
    code: ReplyQualityViolationCode,
    severity: z.enum(["low", "medium", "high"]),
    evidence: z.string().trim().max(160),
  })).max(12),
})
export type ReplyQualityGuard = z.infer<typeof ReplyQualityGuardSchema>

/** 阶段3 暂用的极简 ReplyPolicy 占位（阶段2 接管后替换为真实策略） */
export const ReplyPolicySchema = z.object({
  sentenceBudget: z.object({ min: z.number().int().min(0), max: z.number().int().min(1) }),
  questionLimit: z.number().int().min(0),
  adviceLimit: z.number().int().min(0),
  forbiddenMoves: z.array(z.string()).default([]),
})
export type ReplyPolicy = z.infer<typeof ReplyPolicySchema>
```

---

## 6. Prompt 注入（与 build-prompt.ts 衔接）

`buildAgentChatMessages` 当前 system 段顺序：人设 → 通用约束 → 长期记忆 → 摘要。本阶段在「通用约束」之后、「长期记忆」之前插入**安全策略段**（文章182：本轮安全策略应紧贴角色风格之后、上下文之前）。

改动点（`build-prompt.ts`）：
1. 入参新增可选 `safety?: ConversationSafety | null`。
2. system 段数组新增一行 `getSafetySystemInstruction(safety)`（`continue` 或 null 时返回 `""`，被 `.filter(Boolean)` 自动剔除，零侵入）。

```ts
// build-prompt.ts —— 顶部新增
import type { ConversationSafety } from "@ai-companion/contracts"

export function getSafetySystemInstruction(safety?: ConversationSafety | null): string {
  if (!safety || safety.boundaryAction === "continue") return ""
  return [
    "本轮安全边界判断：",
    `- 等级：${safety.safetyLevel}`,
    `- 分类：${safety.category}`,
    `- 动作：${safety.boundaryAction}`,
    `- 回复策略：${safety.responseGuidance}`,
    "请严格遵守该策略，优先保护用户与他人的现实安全、隐私和关系边界。",
  ].join("\n")
}
```

```ts
// build-prompt.ts —— buildAgentChatMessages 入参与 systemContent 改动
export function buildAgentChatMessages(input: {
  agentDefaultPrompt?: string | null
  safety?: ConversationSafety | null            // ★新增
  activeMemories: ActiveMemory[]
  summary?: string | null
  conversation: InboxChatConversation
  history: HistoryMessage[]
  latestUserText: string
}): ChatCompletionMessage[] {
  const { agentDefaultPrompt, safety, activeMemories, summary, conversation, history, latestUserText } = input

  const systemContent = [
    agentDefaultPrompt || "你是 AI Agent Web 控制台里的聊天陪伴助手。",
    "请基于当前聊天对象、关系氛围和用户意图，用简洁、自然的中文回答用户。",
    "如果用户要求起草回复，请直接给出可发送的聊天内容，避免正式公文格式和职场汇报语气。",
    "你的建议应尊重双方边界，避免操控式话术、制造焦虑或诱导过度解读。",
    getSafetySystemInstruction(safety),          // ★新增，"" 会被 filter 掉
    activeMemories.length > 0 ? [/* 不变 */].join("\n") : "",
    summary ? `此前对话摘要：${summary}` : "",
  ].filter(Boolean).join("\n")
  // …以下不变
}
```

---

## 7. 代码骨架（可直接粘贴）

### 7.1 DeepSeek 非流式 JSON 调用（新增 `services/llm/deepseek.ts` 内或同目录）
```ts
/** 非流式一次性补全，取 message.content 全文（供安全判断结构化解析用） */
export async function callDeepSeekJson(
  config: DeepSeekConfig,
  messages: ChatCompletionMessage[],
  opts?: { signal?: AbortSignal; jsonObject?: boolean },
): Promise<string> {
  const baseUrl = (config.baseUrl ?? "https://api.deepseek.com").replace(/\/$/, "")
  const res = await fetch(`${baseUrl}/chat/completions`, {
    method: "POST",
    headers: { "Content-Type": "application/json", Authorization: `Bearer ${config.apiKey}` },
    body: JSON.stringify({
      model: config.model,
      messages,
      stream: false,
      temperature: 0, // 安全判断要稳定，不要创意（文章182）
      // 退路①：能用就用 json_object；不稳时由 §8 的 prompt 约束 + 解析兜底
      ...(opts?.jsonObject ? { response_format: { type: "json_object" } } : {}),
    }),
    signal: opts?.signal,
  })
  if (!res.ok) {
    const detail = await res.text().catch(() => "")
    throw new Error(`deepseek_http_${res.status}: ${detail.slice(0, 500)}`)
  }
  const json = (await res.json()) as { choices?: { message?: { content?: string } }[] }
  return json.choices?.[0]?.message?.content ?? ""
}
```

### 7.2 安全判断服务 `services/agent-chat/safety.ts`
```ts
import { ConversationSafetySchema, type ConversationSafety } from "@ai-companion/contracts"
import type { DeepSeekConfig, ChatCompletionMessage } from "../llm/deepseek"
import { callDeepSeekJson } from "../llm/deepseek"

const SAFETY_SYSTEM = [
  "你是 AI 电子伴侣聊天产品的安全边界判断器。",
  "你的任务是判断本轮用户输入是否需要安全边界处理，而不是替用户聊天。",
  "必须优先识别自伤危机、违法暴力、隐私侵犯、操控关系、性边界、高风险医疗法律财务建议、强情绪依赖。",
  "不要因为产品是陪伴/恋爱/交友场景就放松边界；也不要过度拦截普通倾诉、轻度暧昧和正常情绪表达。",
  "如果不确定，使用 caution + soft_boundary，而不是 safe。",
  // —— §8 退路：强约束输出（不依赖 response_format）——
  "只输出一个 JSON 对象，禁止任何解释、前后缀、```json 代码块。",
  '字段固定为：{"safetyLevel","category","boundaryAction","reason","responseGuidance","allowMemoryExtraction"}。',
  "safetyLevel∈[safe,caution,redirect,block,crisis]；",
  "category∈[normal,emotional_dependency,manipulation,self_harm,sexual_boundary,privacy,illegal,medical_legal_financial,other]；",
  "boundaryAction∈[continue,soft_boundary,redirect,refuse,crisis_support]；allowMemoryExtraction 为布尔。",
].join("\n")

/** 业务一致性保护：安全等级越高，系统越保守（文章182 normalizeConversationSafety） */
export function normalizeConversationSafety(s: ConversationSafety): ConversationSafety {
  const next = { ...s }
  if (next.safetyLevel === "crisis") { next.boundaryAction = "crisis_support"; next.allowMemoryExtraction = false }
  if (next.safetyLevel === "block" && next.boundaryAction !== "crisis_support") {
    next.boundaryAction = "refuse"; next.allowMemoryExtraction = false
  }
  if (next.boundaryAction === "refuse" || next.boundaryAction === "crisis_support") next.allowMemoryExtraction = false
  if (next.boundaryAction === "continue" && next.safetyLevel !== "safe") next.boundaryAction = "soft_boundary"
  if (!next.responseGuidance) next.responseGuidance = "用温和、克制、尊重边界的方式回复。"
  return next
}

/** 失败保守 fallback：绝不当 safe（文章182） */
export const fallbackSafety: ConversationSafety = {
  safetyLevel: "caution",
  category: "other",
  boundaryAction: "soft_boundary",
  reason: "安全边界判断暂时不可用，采用保守回复策略。",
  responseGuidance: "用温和、克制、尊重边界的方式回复；不要提供操控、伤害、违法或高风险专业建议。",
  allowMemoryExtraction: false,
}

/** 从可能含噪声的文本里抠出第一个 JSON 对象并 zod 解析（§8 退路②） */
function tryParseSafety(raw: string): ConversationSafety | null {
  const m = raw.match(/\{[\s\S]*\}/)
  if (!m) return null
  try {
    const parsed = ConversationSafetySchema.safeParse(JSON.parse(m[0]))
    return parsed.success ? parsed.data : null
  } catch { return null }
}

export async function analyzeConversationSafety(params: {
  config: DeepSeekConfig
  agentName: string
  agentGuardrails: string | null
  activeMemories: { type: string; importance: number; content: string }[]
  recentMessages: { role: "user" | "assistant"; content: string }[]
  userText: string
  signal?: AbortSignal
}): Promise<ConversationSafety> {
  // 空输入直接 safe，省一次调用
  if (!params.userText.trim()) {
    return { safetyLevel: "safe", category: "normal", boundaryAction: "continue",
      reason: "空输入", responseGuidance: "", allowMemoryExtraction: true }
  }
  const human = [
    `Agent 名称：${params.agentName || "未命名 Agent"}`,
    "", "Agent 自定义边界规则：", params.agentGuardrails || "暂无",
    "", "长期记忆：", params.activeMemories.map((m) => `- [${m.type}] ${m.content}`).join("\n") || "暂无",
    "", "最近对话：", params.recentMessages.map((m) => `${m.role}: ${m.content}`).join("\n") || "暂无",
    "", "本轮用户输入：", params.userText,
  ].join("\n")
  const messages: ChatCompletionMessage[] = [
    { role: "system", content: SAFETY_SYSTEM },
    { role: "user", content: human },
  ]

  // §8 退路③：最多 2 次（先 json_object，再纯 prompt 约束），全失败 → fallback
  for (const jsonObject of [true, false]) {
    try {
      const raw = await callDeepSeekJson(params.config, messages, { signal: params.signal, jsonObject })
      const parsed = tryParseSafety(raw)
      if (parsed) return normalizeConversationSafety(parsed)
    } catch (err) {
      console.error("safety analysis attempt failed", { jsonObject, err })
    }
  }
  return fallbackSafety
}

/** 前置短路文本：null 表示不短路（文章182 buildBoundaryResponse） */
export function buildBoundaryResponse(safety: ConversationSafety): string | null {
  if (safety.boundaryAction === "crisis_support") {
    return [
      "我听到你现在可能很难受。先别一个人硬扛，尽量把手边可能伤害自己的东西移远一点，去到更安全、有人能看见你的地方。",
      "如果你有立即伤害自己的可能，请现在联系当地紧急电话或身边可信的人，让他们陪你。你也可以告诉我：你现在是否安全、身边有没有人可以马上联系。",
    ].join("\n\n")
  }
  if (safety.boundaryAction === "refuse") {
    return [
      "这个请求我不能直接帮你完成，因为它可能会伤害他人、侵犯隐私，或越过必要的安全边界。",
      safety.responseGuidance || "我可以换一种更安全、尊重边界的方式，帮你梳理真实需求和可行表达。",
    ].join("\n\n")
  }
  return null
}
```

### 7.3 回复质量守卫 `services/agent-chat/reply-quality.ts`
```ts
import { ReplyQualityGuardSchema, type ReplyQualityGuard, type ReplyPolicy } from "@ai-companion/contracts"
import { normalizeStoredMessage } from "./build-prompt"

export const fallbackReplyPolicy: ReplyPolicy = {
  sentenceBudget: { min: 1, max: 5 }, questionLimit: 1, adviceLimit: 1, forbiddenMoves: [],
}
export const fallbackReplyQualityGuard: ReplyQualityGuard = {
  status: "pass", score: 1, sentenceCount: 0, questionCount: 0, adviceCount: 0, violations: [],
}

const advicePatterns = [/建议你/g, /你可以/g, /不妨/g, /最好/g, /应该/g, /试着/g, /尝试/g, /可以先/g]
const internalLabels = ["intent", "emotion", "route", "policy", "safety", "replyPolicy",
  "意图判断", "情绪路由", "回复策略", "metadata"]
const immersionBreakers = ["作为一个 AI", "作为一个AI", "我只是 AI", "我只是AI", "我是语言模型", "我没有真实情感", "我没有身体"]

const FORBIDDEN_MAP: Record<string, { code: ReplyQualityGuard["violations"][number]["code"]; patterns: RegExp[] }> = {
  lecture: { code: "forbidden_lecture", patterns: [/你要明白/, /你必须/, /正确的做法是/] },
  over_explain: { code: "forbidden_over_explain", patterns: [/换句话说/, /也就是说/, /进一步解释/] },
  premature_advice: { code: "forbidden_premature_advice", patterns: [/你应该立刻/, /马上去/] },
  intense_flirt: { code: "forbidden_intense_flirt", patterns: [/我好爱你/, /没有你我活不下去/] },
  diagnose_user: { code: "forbidden_diagnosis", patterns: [/你这是抑郁/, /你有焦虑症/] },
  take_sides_aggressively: { code: "forbidden_aggressive_siding", patterns: [/他就是渣/, /必须分手/] },
  pressure_to_disclose: { code: "forbidden_pressure", patterns: [/你一定要告诉我/, /必须说清楚/] },
  promise_real_world_action: { code: "forbidden_real_world_promise", patterns: [/我会去找你/, /我帮你处理现实/] },
}

function countSentences(t: string) { return t.split(/[。！？!?\n]+/).map((s) => s.trim()).filter(Boolean).length }
function countMatches(t: string, ps: RegExp[]) {
  return ps.reduce((n, p) => n + (t.match(p)?.length ?? 0), 0)
}

export function evaluateReplyQuality(params: {
  assistantText: string
  replyPolicy?: ReplyPolicy | null
}): ReplyQualityGuard {
  const text = normalizeStoredMessage(params.assistantText)
  if (!text) return fallbackReplyQualityGuard
  const policy = params.replyPolicy ?? fallbackReplyPolicy

  const sentenceCount = countSentences(params.assistantText) // 句数按原文断句，不压缩空白
  const questionCount = countMatches(text, [/？/g, /\?/g])
  const adviceCount = countMatches(text, advicePatterns)
  const violations: ReplyQualityGuard["violations"] = []
  const push = (v: ReplyQualityGuard["violations"][number]) => { if (violations.length < 12) violations.push(v) }

  if (sentenceCount > policy.sentenceBudget.max)
    push({ code: "too_many_sentences", severity: sentenceCount > policy.sentenceBudget.max + 2 ? "high" : "medium",
      evidence: `回复 ${sentenceCount} 句，超过策略上限 ${policy.sentenceBudget.max} 句。` })
  if (questionCount > policy.questionLimit)
    push({ code: "too_many_questions", severity: "medium",
      evidence: `回复含 ${questionCount} 个问句，超过上限 ${policy.questionLimit}。` })
  if (adviceCount > policy.adviceLimit)
    push({ code: "too_many_suggestions", severity: "low",
      evidence: `回复含 ${adviceCount} 处建议型表达，超过上限 ${policy.adviceLimit}。` })
  for (const label of internalLabels)
    if (text.includes(label)) { push({ code: "internal_label_leak", severity: "high", evidence: `泄露内部标签：${label}` }); break }
  for (const phrase of immersionBreakers)
    if (text.includes(phrase)) { push({ code: "breaks_immersion", severity: "high", evidence: `破坏沉浸感：${phrase}` }); break }
  for (const move of policy.forbiddenMoves) {
    const def = FORBIDDEN_MAP[move]
    if (def && def.patterns.some((p) => p.test(text)))
      push({ code: def.code, severity: "medium", evidence: `命中禁止动作：${move}` })
  }

  const high = violations.filter((v) => v.severity === "high").length
  const mid = violations.filter((v) => v.severity === "medium").length
  const low = violations.filter((v) => v.severity === "low").length
  const score = Math.max(0, 1 - high * 0.35 - mid * 0.18 - low * 0.08)
  const status = high > 0 || score < 0.5 ? "fail" : violations.length > 0 ? "warn" : "pass"

  return ReplyQualityGuardSchema.parse({ status, score, sentenceCount, questionCount, adviceCount, violations })
}
```

### 7.4 路由改动 `routes/agent-chat.ts`（关键片段）
```ts
import { analyzeConversationSafety, buildBoundaryResponse } from "../services/agent-chat/safety"
import { evaluateReplyQuality } from "../services/agent-chat/reply-quality"

// —— 读 default_prompt 处一并取 guardrails_prompt ——
const agentRow = await c.env.DB.prepare(
  "SELECT default_prompt, guardrails_prompt FROM user_agent_companions WHERE id = ? AND user_id = ?",
).bind(agentId, userId).first<{ default_prompt: string | null; guardrails_prompt: string | null }>()

const deepseekConfig = {
  apiKey: c.env.DEEPSEEK_API_KEY,
  model: payload.llmConfig?.model ?? c.env.DEEPSEEK_MODEL,
  baseUrl: c.env.DEEPSEEK_BASE_URL,
}

// ★ S1：安全判断（在写用户消息之前）
const safety = await analyzeConversationSafety({
  config: deepseekConfig,
  agentName: payload.conversation.name,
  agentGuardrails: agentRow?.guardrails_prompt ?? null,
  activeMemories,
  recentMessages: history,
  userText: latestUserText,
  signal: c.req.raw.signal,
})

// ① 写用户消息：metadataJson 带 safety
if (latestUserText) {
  sourceUserMessageId = crypto.randomUUID()
  await insertMessage(db, {
    id: sourceUserMessageId, conversationId: conversation.id, userId, agentId,
    role: "user", content: latestUserText, status: "completed",
    metadataJson: JSON.stringify({ analysisVersion: "conversation-safety-v1", safety }),
    nowMs,
  })
  await updateConversationAfterMessage(db, { /* 不变 */ })
}

const promptMessages = buildAgentChatMessages({
  agentDefaultPrompt: agentRow?.default_prompt ?? null,
  safety,                       // ★注入
  activeMemories, summary: conversation.summary,
  conversation: payload.conversation, history, latestUserText,
})

return streamSSE(c, async (sse) => {
  // ★ 前置短路：refuse / crisis_support 不进 DeepSeek
  const boundaryText = buildBoundaryResponse(safety)
  if (boundaryText) {
    await sse.writeSSE({ event: "delta", data: boundaryText })
    const assistantMessageId = crypto.randomUUID()
    const guard = evaluateReplyQuality({ assistantText: boundaryText, replyPolicy: null })
    await insertMessage(db, {
      id: assistantMessageId, conversationId: conversation.id, userId, agentId,
      role: "assistant", content: boundaryText, status: "completed",
      metadataJson: JSON.stringify({ boundaryShortCircuit: true, safety, guard }),
      nowMs: Date.now(),
    })
    // 短路也要把会话计数推进（含本轮 user + 预设 assistant），但不抽记忆（allowMemoryExtraction=false）
    await updateConversationAfterMessage(db, {
      conversationId: conversation.id,
      summary: conversation.summary,
      messageCount: conversation.messageCount + (latestUserText ? 2 : 1),
      lastMessageAtMs: Date.now(), nowMs: Date.now(),
    })
    await sse.writeSSE({ event: "done", data: JSON.stringify({
      conversationId: conversation.id, assistantMessageId, status: "completed" }) })
    return
  }

  // —— 正常流式（阶段1 不变）——
  let assistantText = ""; let failed = false
  try {
    for await (const delta of streamDeepSeekChat(deepseekConfig, promptMessages,
      { temperature: payload.llmConfig?.temperature, signal: c.req.raw.signal })) {
      assistantText += delta
      await sse.writeSSE({ event: "delta", data: delta })
    }
  } catch (err) { failed = true; /* 不变 */ }

  // ⑥ assistant 落库：跑 Q1，metadata 合并 safety + guard
  const assistantMessageId = crypto.randomUUID()
  const afterMs = Date.now()
  const guard = evaluateReplyQuality({ assistantText, replyPolicy: null })
  await insertMessage(db, {
    id: assistantMessageId, conversationId: conversation.id, userId, agentId,
    role: "assistant", content: assistantText, status: failed ? "failed" : "completed",
    metadataJson: JSON.stringify({
      model: deepseekConfig.model, promptMessageCount: promptMessages.length,
      injectedMemories: activeMemories.length, boundaryShortCircuit: false, safety, guard,
    }),
    nowMs: afterMs,
  })

  if (!failed) {
    // ⑦ 摘要（不变）
    // ⑧ 记忆抽取：★ 用 safety.allowMemoryExtraction 做闸门
    if (safety.allowMemoryExtraction) {
      try { /* extractMemoriesFromTurn → saveExtractedMemories，不变 */ } catch (err) { /* 不变 */ }
    }
  }
  await sse.writeSSE({ event: "done", data: /* 不变 */ })
})
```

---

## 8. 结构化输出依赖与退路

**依赖 LLM 结构化输出的判断**：只有 **S1 安全边界判断**（`ConversationSafety`）。
**完全不依赖 LLM 的**：分流 `buildBoundaryResponse`、`normalizeConversationSafety`、整个 Reply Quality Guard（纯代码规则，文章186）。

项目用裸 DeepSeek（无 LangChain `withStructuredOutput`），deepseek-v4-flash 是推理模型、`response_format: json_object` 在三方中转下不一定稳。退路三段（已落进 §7.2 代码）：

1. **Prompt 约束**：system 段明确「只输出一个 JSON 对象、禁止解释/前后缀/代码块」，并把枚举值逐字列出（缩小取值空间，推理模型更易守约）。
2. **zod 解析 + 抠 JSON**：用 `/\{[\s\S]*\}/` 从可能含 reasoning 噪声/```json 包裹的文本里抠出第一个对象，再 `ConversationSafetySchema.safeParse`；解析失败不抛错，返回 null 进入下一次尝试。
3. **重试 + 保守 fallback**：最多 2 次（第 1 次带 `response_format:json_object`，第 2 次去掉、纯靠 prompt 约束，规避中转 API 不支持 json_object 的报错）。两次都拿不到合法对象 → 返回 `fallbackSafety`（caution/soft_boundary/禁抽取），**绝不静默当 safe**（文章182：安全判断失败不等于内容安全）。
4. **规范化兜底**：即便解析成功，`normalizeConversationSafety` 还会修正逻辑不一致（如 crisis 却 continue）（文章182）。

> 升列时机：当 metadata 里 `guard.status='fail'` / 高风险 category 需要做报表或 admin 风控时，再从 `metadata_json` 拆出独立日志表（文章182/186 均建议先 metadata、后拆表）。

---

## 9. API 端点

**本阶段不新增端点**。`POST /agent/chat` 行为增强：
- 请求/响应 SSE 协议不变（`delta` / `error` / `done`），前端无需改动即可工作。
- 短路分支：`delta` 单帧返回预设文本，`done.status` 仍为 `"completed"`。
- 安全/质检结果均落 `metadata_json`，经既有 `GET /agent/conversations/:agentId` 恢复历史时可读取（如需前端展示风险标记，阶段4/admin 再加字段，不属本阶段）。

---

## 10. 实施清单（按 commit 粒度）

- [ ] `feat(contracts): 新增 ConversationSafety / ReplyQualityGuard / ReplyPolicy schema 与导出`
- [ ] `feat(api): DeepSeek 非流式 JSON 补全 callDeepSeekJson`
- [ ] `feat(api): 安全边界判断服务 safety.ts（analyze + normalize + fallback + buildBoundaryResponse）`
- [ ] `feat(api): 回复质量守卫 reply-quality.ts（纯规则检测 + 计分）`
- [ ] `feat(api): build-prompt 注入 getSafetySystemInstruction（safety 入参）`
- [ ] `feat(api): /chat 接入 S1（写用户消息前）+ 前置短路 + assistant metadata 合并 + 记忆抽取闸门`
- [ ] `test(api): 安全判断退路与质检规则单测（refuse/crisis 短路、json 抠取、句数/问号/标签泄露）`

---

## 11. 验证（真实联调）

1. **typecheck**：`export PATH="…/fnm/.../bin:$PATH"; pnpm -w typecheck`（或对应 workspace 的 `tsc --noEmit`），确认 contracts 与三个新服务无类型错误。
2. **本地 D1**：`wrangler dev`（或项目既有启动命令）起 API，确保 `DEEPSEEK_API_KEY/MODEL/BASE_URL` 已配；`user_agent_companions` 有一条带 `guardrails_prompt` 的 Agent。
3. **真实 DeepSeek + 短路验证**：
   - 发「我真的不想活了」→ SSE 应单帧返回危机支持文本、不产生流式逐字；`done.status=completed`。
   - 发「帮我写一段话让对方觉得没有我不行」→ 返回安全拒绝 + 替代方向。
   - 发普通寒暄「今天有点累」→ 走正常 DeepSeek 流式。
4. **查库（安全落库）**：查最近一条 `role='user'` 消息的 `metadata_json`，应含 `analysisVersion=conversation-safety-v1` 与合理 `safetyLevel/boundaryAction`。
   `wrangler d1 execute <DB> --local --command "SELECT role, json_extract(metadata_json,'$.safety.boundaryAction') a FROM agent_conversation_messages ORDER BY created_at_ms DESC LIMIT 4;"`
5. **查库（质检落库）**：最近一条 `role='assistant'` 的 `metadata_json` 应含 `guard.status/score/violations`，且保留阶段1 的 `model/promptMessageCount`。
6. **记忆闸门**：危机/拒绝场景后查 `agent_memories`，本轮**不应**新增记忆；普通偏好场景（如「以后回复我直接一点」且 safety=safe）应正常抽取。
7. **退路验证**：临时把 `DEEPSEEK_MODEL` 改成不支持 `json_object` 的中转模型名或断网 → 安全判断应落到 `fallbackSafety`（caution/soft_boundary），聊天仍可继续、本轮不抽记忆，服务不崩。
8. **质检规则**：构造一条故意超长/多问号/含「作为一个 AI」的 assistant 回复（可临时 mock），确认 `guard.violations` 命中 `too_many_sentences/too_many_questions/breaks_immersion` 且 `status` 正确（high → fail）。